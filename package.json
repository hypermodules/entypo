{
  "name": "entypo",
  "description": "SVG sprite for the Entypo+ icon collection.",
  "version": "2.0.0",
  "author": "Bret Comnes <bcomnes@gmail.com>",
  "bugs": {
    "url": "https://github.com/hypermodules/entypo/issues"
  },
  "devDependencies": {
    "brfs": "^1.4.3",
    "budo": "^10.0.3",
    "gh-release": "^2.2.1",
    "mkdirp": "^0.5.1",
    "npm-run-all": "^4.0.0",
    "rimraf": "^2.5.4",
    "spritesh": "^1.2.0",
    "standard": "*",
    "svgo": "^0.7.1",
    "tap-spec": "^4.0.2",
    "tape": "^4.0.0"
  },
  "homepage": "https://github.com/hypermodules/entypo",
  "keywords": [
    "entypo",
    "icons",
    "sprite",
    "svg"
  ],
  "license": "ISC",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/hypermodules/entypo.git"
  },
  "scripts": {
    "build": "run-s clean optimize make-sprite clean:tmp",
    "clean": "run-p clean:*",
    "clean:dist": "rimraf dist",
    "clean:tmp": "rimraf .tmp",
    "start": "budo example.js --open --live -- -t brfs",
    "make-sprite": "mkdirp dist && spritesh -i .tmp -o dist/sprite.svg --prefix entypo-",
    "optimize": "run-p optimize:*",
    "optimize:base": "svgo -f src/Entypo -o .tmp",
    "optimize:social": "svgo -f src/Entypo\\ Social\\ Extension -o .tmp",
    "prepublish": "npm run build",
    "gh-release": "gh-release",
    "test": "standard && tape test/*.js | tap-spec"
  }
}
